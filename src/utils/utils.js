// department
const showDepartmentChoices = (departmentsFromDB) => {
  return; // get the dept map them //return object key:value key would be the name of dept and value is the id
};

// role
const showRoleChoices = (rolesFromDB) => {
  return;
};

//manager
const showManagerChoices = (managersFromDB) => {
  return;
};
// employee
const showEmployeeChoices = (employeeFromDB) => {
  return;
};

// export the functions to index.js
module.exports = {
  showDepartmentChoices,
  showRoleChoices,
  showManagerChoices,
  showEmployeeChoices,
};
